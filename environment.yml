name: gpaformer
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - numpy
  - scipy
  - nibabel
  - pyyaml
  - tqdm
  - pip:
      - torch>=2.1
      - monai>=1.3
      - einops>=0.6
